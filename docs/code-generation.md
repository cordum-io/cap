# Code generation & the generated-file policy

CAP's protobuf artifacts for six targets (root Go, C++, Node, root Python, SDK
Python, Ruby) are **generated**, held to an exact-bytes manifest, and must never
drift from their `.proto` sources. The full design lives in
[`tools/codegen/README.md`](../tools/codegen/README.md); this page is the policy
and the pin/update procedure.

## Policy

- Generated files are checked in and marked `linguist-generated` in
  `.gitattributes`. Do **not** hand-edit them.
- `tools/codegen/manifest.json` pins every generated output by the sha256 of its
  **LF-normalized** content, so the drift check is identical on a Windows
  (autocrlf CRLF) and a Linux CI (LF) checkout.
- `cap-codegen check` (in `.github/workflows/sdk-gates.yml`) fails on any
  changed, missing, stale, empty, or unmanifested generated file.

## Everyday check (no Docker)

```
go run ./cmd/cap-codegen check
```

## Regenerating (pinned container)

Generation runs in a hermetic image whose supply chain is fully pinned — the
base image by an immutable `@sha256` digest, and every direct `protoc`,
`protobuf-javascript`, and `node` download by a committed per-arch sha256 — with
the network disabled, a read-only source mount, and a fixed
`SOURCE_DATE_EPOCH`/`TZ`/`LC_ALL`. `tools/codegen/pins_test.go` fails if any of
those pins is removed, and runs in both CI workflows:

```
tools/codegen/codegen.sh          # regenerate + verify
tools/codegen/codegen.sh --check  # build image + verify only
# Windows:
pwsh tools/codegen/codegen.ps1
```

After a deliberate regeneration, refresh and review the manifest:

```
go run ./cmd/cap-codegen snapshot
git diff tools/codegen/manifest.json    # review, then commit the generated diff separately
```

## Updating a pin

Edit the `ARG` versions in `tools/codegen/Dockerfile` (protoc, protoc-gen-go,
protoc-gen-go-grpc, grpcio-tools, node). Bumping any version whose artifact is
downloaded directly (`protoc`, `protobuf-javascript`, `node`) also requires
updating its committed per-arch sha256, and bumping the base tag requires
re-resolving the `@sha256` digest
(`docker buildx imagetools inspect golang:<tag> --format '{{.Manifest.Digest}}'`);
`tools/codegen/pins_test.go` fails if a pin or checksum is dropped. Bumping a pin
is expected to change generated bytes — regenerate, review the diff on its own,
and re-snapshot the manifest in the same change.

## Note on the current tree

The checker is live and pins the currently checked-in tree, which has been
reconciled against the merged `.proto` sources: all six targets (root Go, C++,
Node, root Python, SDK Python, Ruby) are regenerated and manifested — C++ and the
others now carry `handshake` and `policy`. `go run ./cmd/cap-codegen check`
reports all 74 outputs matching the manifest.
